# ===== 留言板云端存储（可选）=====
# 两个变量都填了才会启用 Supabase 云端；留空则自动回落到本地文件 .data/messages.json
#
# 取值位置：Supabase 控制台 → Project Settings → API
#   Project URL   → SUPABASE_URL
#   service_role  → SUPABASE_SERVICE_ROLE_KEY
#
# 安全提醒：service_role 是服务端密钥，绝对不要加 NEXT_PUBLIC_ 前缀，
# 也不要 import 进任何带 "use client" 的组件。
#
# 使用方式：把本文件复制成 .env.local，填入下面的值，然后重启 npm run dev
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
